console.log("Hello world!")

/* Para o uso do node.js, usaremos muito o cmd (prompt de comando) e também o terminal dentro do próprio vscode (utilizando a tecla f8) */

/* Podemos ver se o node foi instalado com sucesso e sua versão, escrevendo: node -v, no cmd. Analogamente, podemos fazer npm -v, para saber a versão do npm que está instalada no nosso sistema operacional. */

/* Para usar o node a partir do cmd, precisamos direcioná-lo até o arquivo JavaScript, colocando o endereço dele. Caso o arquivo esteja dentro de uma pasta na área de trabalho, usamos o comando cd, para ele sair do local que está direcionado e direcinarmos-lo para o caminho correto. Assim, podemos fazer, por exemplo: cd Desktop (para direcionar para a área de trabalho) ->
cd NomeDaPasta. Outra forma de direcionar o cmd para a pasta é pegando a pasta e arrastando para o cmd ou entao copiando e colando o endereço dela. Após isso, escrevemos: node NomeDoArquivo. E pronto, o node interpreta e roda o arquivo. */
