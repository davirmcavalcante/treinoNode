var num1 = 10
var num2 = 20

function somar(a, b){
    return a + b;
}

console.log(somar(num1, num2))

/* OBS: o node é um interpretador de JavaScript, então para usá-lo basta saber JavaScript e alguns comandos do cmd. Ademais, por ser um interpretador, ele identifica erros de código do JavaScript. Por exemplo, escreva div no lugar de somar no console.log e veja o erro sendo detectado, pois não criamos nenhuma função chamada div e sim somar, então não podemos chamar a função div. */